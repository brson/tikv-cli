command = {
  SOI ~
  (put | get)
  ~ EOI
}

put = { "put" ~ key ~ value }
get = { "get" ~ key }

key = { string }
value = { string }

string = @{ QUOTATION_MARK ~ ASCII_ALPHANUMERIC* ~ QUOTATION_MARK }

WHITESPACE = _{ " " }
